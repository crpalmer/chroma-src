/**
 * @module RaftError
 */ /** */

import RaftError from "./base";

/**
 * Thrown when an incompatible file extension or MIME type is encountered.
 */
export default class FileTypeError extends RaftError {
    constructor(message?: string) {
        super(message);
        this.name = "FileTypeError";
    }
}
