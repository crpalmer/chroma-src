/**
 * @module RaftError
 */ /** */

import RaftError from "./base";

/**
 * Thrown when an invalid method call is made.
 */
export default class BadMethodCallError extends RaftError {
    constructor(message?: string) {
        super(message);
        this.name = "BadMethodCallError";
    }
}
