/**
 * @module RaftError
 */ /** */

import InvalidArgumentError from "./invalid-argument";

/**
 * Thrown when a state machine parameter is missing or invalid.
 */
export default class InvalidStateParamsError extends InvalidArgumentError {
    constructor(message?: string) {
        super(message);
        this.name = "InvalidStateParamsError";
    }
}
