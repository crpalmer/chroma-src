/**
 * @module RaftInstruction
 */ /** */

import InvalidArgumentError from "../errors/invalid-argument";
import RaftInstruction, {RaftInstructionParams} from "./base";
import {validateNumber} from "../common/validation";

export interface ToggleLightInstructionParams extends RaftInstructionParams {
    on: boolean
}

export default class ToggleLightInstruction extends RaftInstruction {

    readonly on: boolean;

    constructor (params: ToggleLightInstructionParams) {
        super(params);
        this.on = params.on;
    }

    toString(): string {
        return "ToggleLightInstruction";
    }

}
