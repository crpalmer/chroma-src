/**
 * @module RaftInstruction
 */ /** */

import RaftInstruction, {RaftInstructionParams} from "./base";

export interface SDCardInstructionParams extends RaftInstructionParams {
}

export default class SDCardInstruction extends RaftInstruction {

    constructor (params: SDCardInstructionParams) {
        super(params);
    }

    toString(): string {
        return "SDCardInstruction";
    }

}
