/**
 * @module RaftInstruction
 */ /** */

import StabilizeTemperatureInstruction, {StabilizeTemperatureInstructionParams} from "./stabilize-temperature";

interface StabilizeAllTemperaturesInstructionParams extends StabilizeTemperatureInstructionParams {
}

export default class StabilizeAllTemperaturesInstruction extends StabilizeTemperatureInstruction {

    constructor (params: StabilizeAllTemperaturesInstructionParams) {
        super(params);
    }

    toString(): string {
        return "StabilizeAllTemperaturesInstruction";
    }

}
