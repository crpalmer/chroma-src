/**
 * @module RaftInstruction
 */ /** */

import RaftInstruction, {RaftInstructionParams} from "./base";

export interface GetPositionInstructionParams extends RaftInstructionParams {
}

export default class GetPositionInstruction extends RaftInstruction {

    constructor (params: GetPositionInstructionParams) {
        super(params);
    }

    toString(): string {
        return "GetPositionInstruction";
    }

}
