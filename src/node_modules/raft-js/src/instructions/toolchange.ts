/**
 * @module RaftInstruction
 */ /** */

import InvalidArgumentError from "../errors/invalid-argument";
import RaftInstruction, {RaftInstructionParams} from "./base";
import {validateInt} from "../common/validation";

interface ToolChangeInstructionParams extends RaftInstructionParams {
    toolhead: number
}

export default class ToolChangeInstruction extends RaftInstruction {

    readonly toolhead: number;

    constructor (params: ToolChangeInstructionParams) {
        if (!validateInt(params.toolhead) || params.toolhead < 0) {
            throw new InvalidArgumentError("Invalid toolhead '" + params.toolhead + "' supplied as argument");
        }
        super(params);
        this.toolhead = params.toolhead;
    }

    toString(): string {
        return "ToolChangeInstruction";
    }

}
