/**
 * @module RaftInstruction
 */ /** */

import RaftInstruction, {RaftInstructionParams} from "./base";

export interface ProgramEndInstructionParams extends RaftInstructionParams {
}

export default class ProgramEndInstruction extends RaftInstruction {

    constructor (params: ProgramEndInstructionParams) {
        super(params);
    }

    toString(): string {
        return "ProgramEndInstruction";
    }

}
