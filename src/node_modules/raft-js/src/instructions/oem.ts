/**
 * @module RaftInstruction
 */ /** */

import RaftInstruction, {RaftInstructionParams} from "./base";

export interface OEMInstructionParams extends RaftInstructionParams {
}

export default class OEMInstruction extends RaftInstruction {

    constructor (params: OEMInstructionParams) {
        super(params);
    }

    toString(): string {
        return "OEMInstruction";
    }

}
