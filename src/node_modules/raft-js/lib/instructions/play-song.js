"use strict";
/**
 * @module RaftInstruction
 */ /** */
Object.defineProperty(exports, "__esModule", { value: true });
const validation_1 = require("../common/validation");
const invalid_argument_1 = require("../errors/invalid-argument");
const base_1 = require("./base");
/**
 * Play a Tone or Song
 * <p>Instruct the machine to play a preset song using an acceptable song ID.</p>
 */
class PlaySongInstruction extends base_1.default {
    constructor(params) {
        if (!validation_1.validateInt(params.id) || params.id < 0) {
            throw new invalid_argument_1.default("Invalid song ID '" + params.id + "' supplied as argument");
        }
        super(params);
        this.id = params.id;
    }
    toString() {
        return "PlaySongInstruction";
    }
}
exports.default = PlaySongInstruction;
//# sourceMappingURL=play-song.js.map