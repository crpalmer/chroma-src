"use strict";
/**
 * @module RaftInstruction
 */ /** */
Object.defineProperty(exports, "__esModule", { value: true });
const validation_1 = require("../common/validation");
const invalid_argument_1 = require("../errors/invalid-argument");
const base_1 = require("./base");
/**
 * Dwell
 * <p>Pause the machine for a period of time (specified in milliseconds by default, or seconds optionally).</p>
 */
class DwellInstruction extends base_1.default {
    constructor(params) {
        if (!validation_1.validateInt(params.duration) || params.duration < 0) {
            throw new invalid_argument_1.default("Invalid duration '" + params.duration + "' supplied as argument");
        }
        super(params);
        this.duration = params.duration;
        this.useMilliseconds = (params.useMilliseconds === undefined ? true : !!params.useMilliseconds);
    }
    toString() {
        return "DwellInstruction";
    }
}
exports.default = DwellInstruction;
//# sourceMappingURL=dwell.js.map