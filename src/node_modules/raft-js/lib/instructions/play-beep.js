"use strict";
/**
 * @module RaftInstruction
 */ /** */
Object.defineProperty(exports, "__esModule", { value: true });
const validation_1 = require("../common/validation");
const invalid_argument_1 = require("../errors/invalid-argument");
const base_1 = require("./base");
/**
 * Play Beep Sound
 * <p>Play a beep sound, with frequency in Hz and duration in milliseconds.</p>
 */
class PlayBeepInstruction extends base_1.default {
    constructor(params) {
        if (!validation_1.validateInt(params.frequency) || params.frequency < 0) {
            throw new invalid_argument_1.default("Invalid frequency value '" + params.frequency + "' supplied as argument");
        }
        if (!validation_1.validateInt(params.duration) || params.duration < 0) {
            throw new invalid_argument_1.default("Invalid duration '" + params.duration + "' supplied as argument");
        }
        super(params);
        this.frequency = params.frequency;
        this.duration = params.duration;
    }
    toString() {
        return "PlayBeepInstruction";
    }
}
exports.default = PlayBeepInstruction;
//# sourceMappingURL=play-beep.js.map